package de.toxicfox.foxbot.convert.converter;

import java.io.IOException;
import java.io.InputStream;
import java.io.OutputStream;

public class TgsToGif {
     public static void convert(InputStream tgs, OutputStream gif) throws IOException, InterruptedException {
        throw new RuntimeException("TgsToGif conversion is currently not supported.");
     }
}
